---
export interface Props {
  name: string;
  className: string;
}

const { name, className } =  Astro.props;

---

<svg
  class={className}
  aria-hidden="true"
>
  <use href={`/${name}.svg#icon`}  />
</svg>